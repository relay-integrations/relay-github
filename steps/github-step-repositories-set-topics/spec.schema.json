{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
		"token": {
			"type": "string",
			"description": "A secret containing a token with the appropriate permissions."
      "writeOnly": true
		},
		"repos": {
			"type": "array",
      "items": {
        "type": "string"
      },
			"description": "A list of repositories to modify."
		},
    "topics": {
			"type": "array",
      "items": {
        "type": "string"
      },
      "description": "The topics to add or replace with.",
    },
    "mode": {
      "type": "string",
      "description": "Whether to replace or add. (replace, add)",
      "default": "replace"
    }
	},
	"required": [
		"token",
    "repos",
    "topics"
	],
	"additionalProperties": false
}
