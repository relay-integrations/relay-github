# repositories-set-topics

This step will add or replace topics associated with a list of repositories. It requires a
token with the proper access, otherwise you'll very quickly hit GitHub API rate-limiting.
