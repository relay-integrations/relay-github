{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
		"token": {
			"type": "string",
			"description": "A secret containing a token with the appropriate permissions."
      "writeOnly": true
		},
		"repos": {
			"type": "array",
      "items": {
        "type": "string"
      },
			"description": "A list of repositories to validate."
		},
		"path": {
			"type": "string",
			"description": "The path of the file to check for content. Exclusive to path_regex."
		},,
		"path": {
			"type": "string",
			"description": "The string form of a regex matching expected forms of the file path. Exclusive to path."
		},
    "content": {
      "type": "string",
      "description": "The string form of the regex to match against file content",
      "default": "all"
    },
    "match": {
      "type": "string",
      "description": "The type of matching to use. (positive, negative)",
      "default": "positive"
    }
	},
	"required": [
		"token",
    "repos",
    "content"
	],
	"additionalProperties": false
}
